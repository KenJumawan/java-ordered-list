# java-ordered-list
This project is a Java implementation of a generic ordered linked list that keeps elements sorted using natural ordering. It supports insertion, removal, indexed access, merging, and retrieving the k largest elements. The project also includes a sales reporting application that uses the list to process and report top sales values from input data.
